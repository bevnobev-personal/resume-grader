Day 1: Did all design work in a doc, no code. Felt slower than expected — I kept wanting to skip to building. Notable: had to research ATS keyword behavior to make defensible decisions about the grading logic. That research changed my design.
Ran gh repo create --source=. --remote=origin --push with an existing broken origin remote. gh created the repo on GitHub but silently (or noisily, depending on your version) failed to overwrite the remote. Result: split-brain state — repo exists, local doesn't know about it. The fix was obvious once I checked git remote -v and gh repo view, but the failure mode itself wasn't loud enough. A better tool would have either: refused to proceed with conflicting state, or done a forced overwrite with a clear warning. Instead I got a half-completed state and had to diagnose.
